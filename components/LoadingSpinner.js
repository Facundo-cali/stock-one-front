import React from 'react';

const LoadingSpinner = () => (
  <>
    <div className="spinner" id="my-spinner"></div>
    <h2>Al ser un servidor gratuito, si no se ha usado en un tiempo, Azure lo apaga y tarda un poco en encenderlo de nuevo. Por favor, espere unos segundos.</h2>
  </>
);

export default LoadingSpinner;